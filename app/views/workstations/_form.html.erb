<%= simple_form_for(@workstation, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.association :location, label_method: :room %>
  <%= f.input :number, label: 'Workstation number' %>
  <%= f.association :workstation_type %>
  <%= f.button :submit %>
<% end %>
