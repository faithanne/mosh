<%= simple_form_for(@location, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :room %>
  <%= f.association :location_type %>
  <%= f.input :comment %>
  <%= f.button :submit %>
<% end %>
