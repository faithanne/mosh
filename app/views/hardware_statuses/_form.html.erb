<%= form_for(@hardware_status) do |f| %>
  <% if @hardware_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hardware_status.errors.count, "error") %> prohibited this hardware_status from being saved:</h2>

      <ul>
        <% @hardware_status.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
